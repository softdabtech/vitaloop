name: CI / CD

permissions:
  contents: read

concurrency:
  group: cicd-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ─────────────────────────────────────────────────────────
  # JOB 1: Backend lint + unit tests (no Supabase needed)
  # ─────────────────────────────────────────────────────────
  backend-test:
    name: Backend — lint & unit tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: ./backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Syntax-check all .py files
        run: |
          python - <<'EOF'
          import ast, os, sys
          errors = []
          for root, dirs, files in os.walk("app"):
              dirs[:] = [d for d in dirs if d != "__pycache__"]
              for f in files:
                  if not f.endswith(".py"):
                      continue
                  path = os.path.join(root, f)
                  with open(path) as fh:
                      src = fh.read()
                  try:
                      ast.parse(src)
                  except SyntaxError as e:
                      errors.append(f"{path}: {e}")
          if errors:
              for e in errors:
                  print("SYNTAX ERROR:", e)
              sys.exit(1)
          print(f"Syntax OK — {sum(1 for r,d,fs in os.walk('app') for f in fs if f.endswith('.py'))} files checked")
          EOF

      - name: Run unit tests
        env:
          # Stub values so modules that guard against missing config pass import
          SUPABASE_URL: "https://placeholder.supabase.co"
          SUPABASE_SERVICE_ROLE_KEY: "placeholder"
        run: |
          pytest tests/test_analyze.py \
                 tests/test_router_input_guards.py \
                 tests/test_rate_limit_middleware.py \
                 -q --tb=short

  # ─────────────────────────────────────────────────────────
  # JOB 2: Frontend build check
  # ─────────────────────────────────────────────────────────
  frontend-build:
    name: Frontend — build check
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build production bundle
        run: NODE_OPTIONS=--max-old-space-size=1536 vite build --mode production

  # ─────────────────────────────────────────────────────────
  # JOB 3: Deploy to production (only on push to main)
  # ─────────────────────────────────────────────────────────
  deploy:
    name: Deploy to production
    runs-on: ubuntu-latest
    needs: [backend-test, frontend-build]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Set up SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.PROD_SSH_KEY }}

      - name: Add server to known hosts
        run: |
          ssh-keyscan -H ${{ secrets.PROD_HOST }} >> ~/.ssh/known_hosts

      - name: Pull latest code on server
        run: |
          ssh root@${{ secrets.PROD_HOST }} "
            cd /var/www/VITALOOP &&
            git pull origin main
          "

      - name: Restart backend service
        run: |
          ssh root@${{ secrets.PROD_HOST }} "
            systemctl restart vitaloop-backend &&
            sleep 4 &&
            systemctl is-active vitaloop-backend
          "

      - name: Build and deploy frontend
        run: |
          ssh root@${{ secrets.PROD_HOST }} "
            cd /var/www/VITALOOP/frontend &&
            NODE_OPTIONS=--max-old-space-size=1536 npm run build:prod &&
            cp -r dist/* /var/www/html/ &&
            nginx -s reload
          "

      - name: Health check
        run: |
          sleep 5
          status=$(curl -sf -o /dev/null -w "%{http_code}" https://${{ secrets.PROD_HOST }}/health || echo "000")
          echo "Health endpoint returned: $status"
          if [[ "$status" != "200" ]]; then
            echo "Health check FAILED (status=$status)"
            exit 1
          fi
          echo "Health check PASSED"
