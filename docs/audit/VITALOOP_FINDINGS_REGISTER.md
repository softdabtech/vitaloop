# VITALOOP Findings Register

**Audit date:** 2026-08-28
**Scope:** Live production (`/var/www/VITALOOP`) + live staging (`/opt/vitaloop-staging`), server SoftDAB.
**Method:** Static trace of the deployed working-tree code (not git HEAD) + live read-only DB/schema introspection + live HTTP execution of a real end-to-end journey against production using a user-provided test account (`zzz@z.com`).
**Severity key:** P0 = safety/security/data-corruption/false-interpretation/broken-core-flow. P1 = major UX/functionality/integrity/reproducibility/retention failure. P2 = tech debt/maintainability. P3 = polish/cleanup, or anything that is *only* git/history drift with no live-runtime effect.
**Status key:** all findings are **OPEN** — no code has been changed as part of this audit.

| ID | Sev | Area | User flow | Finding | Evidence | Repro | Expected | Actual | Root cause | Category | Confidence | Proposed direction | Regression test |
|----|-----|------|-----------|---------|----------|-------|----------|--------|------------|----------|------------|---------------------|------------------|
| F01 | **P0** | Analysis pipeline | Lab upload → report | Quality gate scores a `block_or_confirm` decision but the pipeline never blocks or requires confirmation before persisting/serving a full report. | `backend/app/services/analysis_quality_gate.py:52-55` (docstring admits "does not block legacy flows yet"); `lab_analysis_pipeline.py:917-959` always returns `status:"completed"`. **Live-reproduced**: Lab #1 in the production journey returned `decision:"block_or_confirm", requires_confirmation:true` with 2 blockers, yet biomarkers/protocol/report were already persisted and served in the same response; all `biomarker_extraction_candidates` rows stayed `status:"pending"` forever with no confirm step ever occurring. | Upload any lab text producing a low-confidence/implausible candidate via `POST /analyze`; observe `analysis_input_quality_gate.decision` vs. whether the same response already contains a full report. | Low-confidence/conflicted input is held pending explicit user confirmation before a confident report is generated. | A confident-looking report is always generated and shown immediately regardless of gate decision. | Gate is advisory-only by design ("intentionally conservative... produces a decision artifact"); nothing in `analyze.py` branches on it. | Medical safety, product logic | Confirmed (code + live) | Make `analyze.py`'s report-serving path actually branch on `decision`; require an explicit confirm call before persisting/serving `interpreted_report`/`protocol` when `block_or_confirm`. | Add integration test: low-confidence upload → assert report fields withheld/pending until `POST /confirm-candidates`. |
| F02 | **P0** | Safety engine | Lab upload → report | Safety-engine "blocked"/high-severity flags never withhold content from the live API response. | `backend/app/services/safety/safety_engine.py:230-353`; `analyze.py` (7+ response-construction sites never branch on `safety_result`). **Live-reproduced**: both journey uploads triggered `safety_events:[{"key":"pediatric_context","severity":"high"}], doctor_discussion_required:true`, yet `status` was `"approved_with_warnings"`, `blocked_items:[]`, and the full report/protocol were persisted and served regardless. | Submit a lab for a pediatric-context profile; observe `safety_result.status` vs whether the report is served. | A "high severity"/blocked safety event withholds or materially alters the served content, not just tags it. | Content is served unconditionally; only a DB `report_versions.status` column reflects "blocked" after the fact, invisible to the live response. | Safety engine computes a verdict but no caller enforces it as a gate, except one narrow pediatric/pregnancy dosage-text substitution. | Medical safety | Confirmed (code + live) | Enforce `safety_result.status == "blocked"` as a hard withhold in the API layer, not just a persistence-layer tag. | Integration test: profile with pediatric age + dangerous lab value → assert report is withheld/redacted, not just flagged. |
| F03 | **P0** | Longitudinal / Progress | Dashboard, `/lab-results` (the actual routed "Progress" experience) | The live-routed progress data path (`/progress`, via `get_user_progress()`) is ordered by `created_at` (upload time), not by real lab `test_date` — the exact anti-pattern the product rules forbid. | `backend/app/services/supabase_service.py:1260-1271`, `.order("created_at", desc=True)`; consumed by `dashboard.py:407-514` for `/dashboard/summary`'s `progress`/`latest_upload`, and directly by `LabResultsList.jsx` (`api.get('/progress')`, confirmed the only frontend caller of this endpoint — `App.jsx:798` routes `/progress` → `/lab-results` → this file). **Not yet reproduced with an out-of-order upload in the live journey** (both test uploads happened close together in wall-clock order matching `test_date` order) — confirmed from code, not yet forced live. | Upload a lab with `test_date` older than an already-uploaded lab's `test_date`; check whether `/progress`'s ordering / `dashboard.summary.latest_upload` reflects test-date order or upload order. | "Latest result" and trend order always reflect real lab dates. | Ordering follows upload time; an out-of-order backfill will silently misorder "latest result" for real users. | `get_user_progress` was written/patched before the `test_date`/`collected_at`/`reported_at` columns existed and was never updated to use them, unlike the newer `progress_overview.py` (`/progress/overview`), which is correctly `test_date`-driven and live-verified accurate (`mode:"high_confidence_time_trend"`, dates matched exactly). | Product logic, data integrity | Confirmed (code); live-reproduction of the misordering itself pending | Change `get_user_progress`'s `.order()` to use the same `test_date or collected_at or reported_at` fallback already used elsewhere, or retire the legacy `/progress` endpoint in favor of `/progress/overview` everywhere it's consumed. | Add test: two uploads, older `test_date` uploaded second → assert `/progress`/`dashboard.summary.latest_upload` reflects `test_date` order. |
| F04 | P1 | Analysis pipeline | Lab upload → report | Extraction hallucinated a biomarker from a date string in the source text. | Live journey: source text line `"Report Date: 2026-07-29"` was extracted by the LLM as biomarker `{"name":"Report Date","value":2026.0,"unit":"-","ref_low":7.0,"ref_high":29.0,"status":"ELEVATED"}`, persisted to `biomarkers` (id `6ea979f7-...`), surfaced on `/dashboard/summary` and `/progress/overview`, and was the direct cause of that upload's `unit_or_plausibility_conflict` quality-gate blocker. | `POST /analyze` with any lab text containing a "Report Date:" or similar date line. | Non-biomarker metadata lines are filtered before/after LLM extraction. | A fabricated "biomarker" with an absurd reference range (7–29) reaches the user-visible report. | No post-extraction plausibility/allowlist filter on marker names; LLM free-parses arbitrary text lines. | Data integrity, extraction quality | Confirmed (live) | Add a biomarker-name allowlist/denylist filter and a plausibility check (e.g. reject `ref_high` values that are actually calendar-like) before candidates are persisted. | Fixture: lab text with a "Report Date:" line → assert no biomarker named "Report Date" is created. |
| F05 | P1 | Dashboard / Check-in loop | Weekly check-in | `/dashboard/summary` is a byte-identical no-op immediately after a successful check-in submission. | Live journey: `stats` (`health_score`, `streak_days`, `insights_count`, `goals_achieved`) and `next_best_action` (still "Run weekly check-in") were identical before/after `POST /checkins`, even though the row was confirmed persisted in `checkins_weekly`. | Submit a check-in via `POST /checkins`; diff `GET /dashboard/summary` before/after. | Check-in completion visibly changes at least `next_best_action` and/or a streak/adherence-linked stat. | Dashboard summary fields aren't recomputed synchronously from the just-submitted check-in (though check-in data does feed `calculate_health_score`'s adherence component and dashboard *insights* generation per the backend trace — those paths weren't triggered/visible in this single-session window). | Product loop / retention | Confirmed (live) | Recompute or invalidate cached `health_score`/`next_best_action` synchronously on check-in submit, or clearly document/display the recompute cadence to the user. | Test: submit check-in → assert `next_best_action` or a streak/adherence field changes within the same session. |
| F06 | P1 | Frontend — Progress page | Dead but present code | `Progress.jsx`'s "Improving"/"Top improvements" classification is computed purely from the arithmetic sign of raw value change, with no clinical-direction awareness — a rising LDL/glucose/CRP would show as "Improving". **Confirmed not currently reachable**: `/progress` route redirects to `/lab-results` (`App.jsx:798`), and `Progress.jsx` has zero imports anywhere in the codebase. | `frontend/src/pages/Progress.jsx:229-234,376-377`; routing confirmed via `App.jsx:798` and repo-wide import grep. | N/A live (dead code) — read the file directly. | If/when this page is ever re-enabled, direction of "improvement" must be clinically aware. | Component computes "Improving" from `delta >= 0` only. | Product/medical-safety logic (latent) | Confirmed (code); confirmed unreachable live | Do not re-wire this route without first making trend direction backend-owned and clinically aware (or delete the dead component). | Before ever re-enabling `/progress` → `Progress.jsx`: unit test asserting a rising LDL/glucose is never labeled "Improving". |
| F07 | P1 | Frontend — Dashboard | Post-login dashboard | "Health Signal Score" panel shows partially fabricated percentages: `biomarkerScore` is a flat constant (70 if any results exist, else 25) and `safetyScore` is binary 85/45 from a substring match — not derived from actual biomarker values. | `frontend/src/pages/UserDashboard.jsx:73-77` | Load the dashboard for any user with results; inspect the four score bars against actual lab data. | Displayed clinical-sounding scores are computed from real data. | Two of four scores are hardcoded/near-constant regardless of actual biomarker severity. | Placeholder logic shipped as if final | UX / product-integrity | Confirmed (code) | Replace with a real computation sourced from biomarker status distribution and safety_result, or clearly label as illustrative/remove until real. | Snapshot test: two users with opposite lab severity must produce different `biomarkerScore`. |
| F08 | P1 | Report reproducibility | Viewing a historical report | `GET /{upload_id}` recomputes the entire pipeline live against *current* KB/prompt/model config instead of serving the frozen `report_versions` row, defeating the reproducibility the versioning table is designed to guarantee. | `backend/app/routers/analysis/analyze.py:1429` (`get_results`), explicit comment "Build the current report on read so older uploads are upgraded without re-uploading." | Generate a report, change a KB rule/prompt, re-open the same `upload_id`. | The originally-generated, frozen report is what's shown, or a clear "updated" indicator/diff is shown. | Report content silently changes on every KB/prompt/model change; the immutable `report_versions` row is fetched separately but isn't the primary content shown. | Deliberate "always fresh" design choice that conflicts with reproducibility goals | Reliability / reproducibility | Confirmed (code) | Serve the frozen `report_versions` row as primary content; offer an explicit "regenerate with current knowledge base" action instead of silent recompute-on-read. | Test: generate report, mutate KB version constant, re-fetch same upload → assert content unchanged unless regeneration explicitly requested. |
| F09 | P1 | Data model / traceability | Whole pipeline | No `analysis_id` concept exists anywhere; the de facto chain key `upload_id` has **no FK constraint** on any of the 7 newest pipeline tables. | `backend/sql/stage-25-analysis-quality-gate-and-report-provenance.sql` — `biomarker_extraction_candidates`, `report_versions`, `safety_events`, `analysis_quality_gates`, `clinical_data_integrity_events`, `evidence_gaps`, `health_state_versions` all declare `upload_id uuid not null` with no `references`. Live schema introspection confirms these tables exist and are in active use (populated during the live journey). | Attempt to insert one of these rows with a nonexistent `upload_id` (not attempted destructively in this audit — inferred from schema). | Orphan rows are impossible at the DB layer. | Nothing prevents an orphaned quality-gate/report/safety row if its `lab_uploads` parent is ever deleted or the id is wrong. | FKs omitted when these tables were added (`stage-25`), unlike older `biomarkers`/`protocols` which do have FKs. | Data integrity | Confirmed (code + live schema) | Add `references public.lab_uploads(id) on delete cascade` (or `restrict`, per retention policy) to all 7 tables. | Schema test asserting FK presence on all upload-linked tables. |
| F10 | P1 | Entitlement / billing | Premium gating, admin/CRM | Premium/subscription status has multiple partially-reconciled sources of truth, one of which (`users.plan_tier`) doesn't exist as a live database column at all despite being actively read/written by app code. | `backend/app/services/entitlements.py:9-58,96` (comment: "Deprecated compatibility source; prefer canonical subscriptions rows"; `needs_sync` flag exists because sources diverge); live PostgREST schema introspection of `public.users` confirms no `plan_tier` column exists. `supabase_service.py` has defensive code specifically handling the case where a `plan_tier` write fails because the column is absent. | Compare `resolve_user_entitlements()`'s dual-source logic against the live `users` table schema. | One canonical entitlement source, enforced by schema. | Four overlapping fields (`sub_status`, `subscription_status`, phantom `plan_tier`, `subscriptions.status`) with reconciliation logic and a self-documented sync-drift flag. | Incremental patches over time without consolidating the schema. | Billing/entitlement, data integrity | Confirmed (code + live schema) | Pick `subscriptions` as sole canonical source; migrate `users.*` fields to a read-only cache populated by trigger, or remove them; add the missing `plan_tier` column for real or stop referencing it. | Test: entitlement resolves identically regardless of which field an admin sets. |
| F11 | P1 | Database integrity | Symptoms feature (any) | Live `public.symptoms` table has two structurally unrelated column sets merged onto one table with no discriminator: the per-user symptom-log shape (`user_id, upload_id, tags, severity`) and the knowledge-catalog shape (`key, name, description, severity_scale, metadata`). | Live PostgREST schema introspection: `definitions.symptoms.properties` returns all 11 columns from both original migration files (`supabase_migrations.sql:79` vs `stage-18-knowledge-base-foundation.sql:61`) merged. | Query `public.symptoms` schema (read-only `GET {SUPABASE_URL}/rest/v1/`). | One table, one purpose, or two separate tables. | One live table silently serves two unrelated purposes with no way to distinguish row "type" other than which columns happen to be populated. | Two independently-written migrations targeting the same table name, hand-reconciled at some point outside of tracked migration files. | Data model, maintainability | Confirmed (live schema) | Split into two tables (`symptom_log`, `symptom_catalog`) or add an explicit `record_type` discriminator; backfill and update all call sites. | Schema test asserting column-set consistency / a discriminator exists. |
| F12 | P1 | Check-in data model | Weekly check-in traceability | Check-in data cannot be joined back to the specific report/analysis it pertains to for most users; two parallel, non-unified check-in systems exist. | `checkins_weekly` (`supabase_migrations.sql:250`) has no `upload_id`/`report_version_id` at all — only `user_id`+`week_start`. A separate `symptom_checkins` (`stage-15-health-loop-core.sql:135`) links only via `concern_id`→`health_concerns`, not to `report_versions`. Both are live tables (confirmed via schema introspection); the live journey's `POST /checkins` wrote to `checkins_weekly`. | Submit a check-in, then attempt to determine which report/upload it was "checking in on." | Check-in is traceable to the active protocol/report version at time of submission. | No structural link exists; only `week_start` timing is available. | Two independently-built check-in features, never unified. | Data model, longitudinal review | Confirmed (code + live) | Add `report_version_id` (nullable, "active protocol at time of check-in") to `checkins_weekly`; consider merging with `symptom_checkins` or documenting the split as intentional. | Test asserting a check-in can be joined to the protocol it was submitted against. |
| F13 | P2 | AI provider config | Ops/observability | Production env carries three fully dead AI-provider credential sets (`DIGITALOCEAN_CLAUDE_*` — labeled "Primary for lab analysis" — `ABACUS_AI_*`, `ROUTELLM_*`) referenced by zero lines of code; only `OPENAI_*` is active. | `backend/app/config.py:107-119` (`active_llm_*` hardcoded to `openai_*` only); live env values for the other three are empty strings; repo-wide grep for their names returns zero hits in `app/`. | Read `/etc/vitaloop/backend.env`; grep `app/` for the var names. | Env only contains config that's actually wired up, or is clearly marked deprecated. | Misleading operational config implies a fallback/primary provider that doesn't exist in code. | Historical provider-migration cruft never cleaned up. | Observability, tech debt | Confirmed (live) | Remove the three dead var groups from the env file (or comment them `# UNUSED, kept for historical reference`) and from `config.py`'s `Settings` model. | N/A (config cleanup, not a code-behavior test). |
| F14 | P2 | Naming/tech debt | Extraction pipeline | Active OpenAI extraction code lives in files named `claude_service.py`/`claude_pdf_analyzer.py`, with `openai_service.py`/`openai_pdf_analyzer.py` as thin re-export shims — confusing for anyone tracing the pipeline. | `backend/app/services/ai/openai_service.py:8-9`, `openai_pdf_analyzer.py:7-8`. | Read the files. | Module names match the provider actually called. | Vestigial "Claude" naming from a prior provider. | Incomplete rename during a provider migration. | Tech debt | Confirmed (live) | Rename `claude_service.py`→the real implementation module, retire the re-export shims once all imports are updated. | N/A. |
| F15 | P2 | Analysis service architecture | OCR pipeline | `AnalysisServiceClient` (targeting the separate OCR microservice on port 8006) is instantiated as a live module-level singleton but is never called anywhere — dead code pointing at a service that IS running (`analysis-service.service`, confirmed active) but is bypassed by the real pipeline (OpenAI vision handles PDFs/images directly). | `backend/app/services/analysis_service.py`; repo-wide grep confirms zero callers. | Read the file; grep for callers. | Either the OCR service is actually used, or the client/service is retired. | A whole running systemd service (`analysis-service.service`, port 8006, tesseract) sits idle/unused by the main pipeline. | Architecture pivot (moved to LLM-vision extraction) without decommissioning the older OCR service. | Tech debt, resource waste | Confirmed (live) | Decide: either wire the OCR service in as a genuine fallback for LLM-vision failures, or decommission `analysis-service.service` and delete the dead client. | N/A. |
| F16 | P2 | Frontend duplication | Report/results rendering | `Results.jsx` duplicates backend clinical-range classification client-side (`inferStatusFromRange()`, arbitrary 15%-of-span borderline margin) whenever the backend omits a `status` field. | `frontend/src/pages/Results.jsx:191-206`. | Any biomarker response missing `status`. | Range classification is backend-owned, single source of truth. | Frontend fallback added defensively, threshold not guaranteed to match backend. | Single-source-of-truth risk | Confirmed (code) | Ensure backend always populates `status`; remove/deprecate the client fallback or keep it pinned to an explicitly-shared constant. | Contract test: backend `status` field always present for populated biomarkers. |
| F17 | P2 | API surface | All | Duplicate near-identical route groups live simultaneously: `/questionnaire/*` vs `/questionnaires/*`, `/auth/entitlements` vs `/users/entitlements`, `/auth/subscription` vs `/users/subscription`, `/auth/onboarding/organization` vs `/users/onboarding/organization`, `/red-flags` vs `/admin/red-flags`. Confirmed only the singular `/questionnaire/*` and `/auth/*` groups are actually called by the frontend. | Live `openapi.json` (159 paths) + `grep` of `frontend/src` for actual call sites. | Diff the OpenAPI path list against frontend API call sites. | One canonical route per resource. | Plural/duplicate groups remain live, reachable, undocumented as deprecated. | Incremental API evolution without removing superseded routes. | API contract, tech debt | Confirmed (live) | Mark the unused duplicate groups deprecated in OpenAPI, then remove after a grace period. | N/A. |
| F18 | P2 | Frontend quota UX | Lab upload | Upload dropzone is never `disabled` by quota/premium state; `handleFile()` calls `POST /analyze/pdf` unconditionally before any client-side quota check — relies entirely on a post-hoc backend 402. | `frontend/src/pages/Upload.jsx:280-320,461-479`. | Free user at quota limit attempts an upload. | Upload blocked client-side before any expensive call. | Full upload + OCR/AI attempt proceeds, then (if backend enforces it — confirmed backend does check before AI calls, see entitlements trace) fails after the fact. | Cosmetic-only gating banner, not wired to the dropzone's `disabled` state. | UX, cost efficiency | Confirmed (code) | Disable the dropzone client-side when quota is exhausted and not premium. | UI test: quota-exhausted free user cannot trigger a file input change handler. |
| F19 | P3 | Ops/staging | Staging environment | Staging backend's configured Supabase project (`dcmnnqlrcmhgmhqznpet.supabase.co`) has no public DNS record — every authenticated request has failed for an unknown period. Staging also has zero LLM provider keys configured. | Live: staging error log shows `PyJWKClientConnectionError` DNS failures on every JWT validation attempt; `grep -c "completed with 200"` across the log for authenticated routes = 0; `.env.staging`'s 4 LLM key vars all empty. | `GET /auth/me` on `staging-api.vitaloop.today` with any bearer token. | Staging has a working, isolated backing store and can exercise the real LLM pipeline. | Zero authenticated requests can succeed; extraction would silently fall back to regex parsing even if auth were fixed. | Unknown — likely an abandoned/rotated Supabase project reference. | Reliability, environment parity | Confirmed (live) | Provision a real dedicated staging Supabase project and a staging-only LLM key; update `.env.staging`; restart the service. | Add an uptime/synthetic-auth check for staging so this doesn't silently persist. |
| F20 | P3 | Test coverage | Whole cabinet | The live server's Playwright E2E suite (`frontend/tests/`, 13 spec files) and the CRM's xUnit test project (`crm-mvc/tests/Vitaloop.Crm.Web.Tests/`) do not exist on disk, and `playwright.config.ts` still points `testDir: './tests'` at that now-missing directory — `npx playwright test` fails outright as currently deployed. This is a live test-coverage gap, not a repo-cleanliness issue. | `ls frontend/tests/` → no such directory; `playwright.config.ts:4` `testDir: './tests'`. | Run `npx playwright test` in `/var/www/VITALOOP/frontend`. | E2E suite runs and provides regression coverage for the cabinet flows audited here. | Command fails immediately — no working automated E2E coverage exists on this host today. | Files absent from the working tree (git-drift origin, but the *consequence* — no live coverage — is itself a current-state fact). | Test architecture | Confirmed (live) | Restore/rebuild the Playwright suite (or point CI/local runs at wherever it now actually lives, if it was intentionally relocated) so automated coverage exists on this host again. | The E2E suite itself is the regression test — first goal is simply making it runnable again. |
| F21 | P3 | Reliability | Payments | Production has zero payment-gateway keys configured (Stripe removed, a Paddle migration attempted Aug 24 and rolled back Aug 25 per `.env` backup files) — this is confirmed **intentional**, not broken-in-place; no user can currently pay on production, which is a revenue question, not a safety/security one. | `/etc/vitaloop/backend.env` — no `STRIPE_*`/`PADDLE_*` keys; `backend.env.pre-paddle-rollback-20260825120226` backup shows the attempted-then-reverted Paddle config. | Attempt any checkout flow on production. | N/A — confirm product decision. | No live checkout path exists; entitlement is 100% manual (CRM/admin). | Deliberate interim state during a payment-provider migration. | Billing, product decision (not a defect) | Confirmed (live) | Not a code fix — a product decision on when/how to re-launch billing. Flag for stakeholder awareness only. | N/A. |
| F22 | P3 | Live nginx | Routing | The live `/etc/nginx/sites-enabled/vitaloop.today` still has a `location /stripe/webhook → 127.0.0.1:8004/stripe/webhook` proxy target, but the backend has no such route mounted — any real call 404s. Not a defect by itself (nothing calls it), documented for completeness per the "audit what's actually running" directive. | `grep proxy_pass /etc/nginx/sites-enabled/vitaloop.today`; confirmed no `/stripe/*` router mounted in live `main.py`. | `curl -X POST https://vitaloop.today/stripe/webhook` | N/A | 404 | Leftover from Stripe removal. | Config drift (live, not git) | Confirmed (live) | Remove the dead nginx location block once billing direction is finalized. | N/A. |

**Not-yet-reproduced items flagged for a follow-up live pass** (explicitly separated per instructions to distinguish CONFIRMED vs POTENTIAL):
- F03's actual user-visible misordering (needs an out-of-order `test_date` upload specifically against the *legacy* `/progress` endpoint, not `/progress/overview`).
- Whether `check-ins`/`insights` recompute asynchronously on a longer delay than the single-session window tested (F05) — the live journey only checked immediately before/after.
- Whether the quality gate or safety engine ever produces a hard block for genuinely severe/impossible inputs (both live test uploads produced `approved_with_warnings`/`block_or_confirm`, never `blocked`/an actual halt) — worth a deliberately extreme test input (e.g., glucose=9999) to see if a true hard-stop exists anywhere.
