# Local staging-only variables. Do not commit real secrets.

STAGING_DATABASE_URL="postgresql://user:pass@host:5432/postgres"
STAGING_API_URL="https://staging-api.vitaloop.today"
SMARTLAB_PARTNER_API_KEY="replace-with-staging-smartlab-key"

# Required by backend/scripts/seed_partner_smartlab.py (service-role access).
SUPABASE_URL="https://your-staging-project-ref.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="replace-with-staging-service-role-key"

# Stage 18 knowledge rollout/smoke
# Super-admin JWT for staging API calls.
STAGING_SUPER_ADMIN_BEARER="replace-with-staging-super-admin-jwt"
# UUID of the same super-admin user (used in governance approve payloads).
STAGING_SUPER_ADMIN_USER_ID="00000000-0000-0000-0000-000000000000"
